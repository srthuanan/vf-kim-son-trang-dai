import { supabase } from './src/services/supabaseClient'; async function test() { const res = await supabase.from('hr_leave_requests').update({status: 'pending_director'}).eq('id', '1'); console.log('UPDATE hr:', res.error); const res2 = await supabase.from('interactions').insert({category: 'NOTIFICATION', type: 'info', recipient: 'dummy', message: 'test', actor_name: 'test', target_view: 'hr', target_id: '1'}); console.log('INSERT interaction:', res2.error); } test();
